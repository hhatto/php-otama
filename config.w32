// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("otama", "for otama support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("otama", "enable otama support", "no");

if (PHP_OTAMA != "no") {
	EXTENSION("otama", "otama.c");
}

